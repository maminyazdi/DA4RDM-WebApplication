def update_xes_columns(new_columns, session_id):

    return

def update_filters(new_filters, session_id):

    return

def get_column_names(dataframe):
    columns = list(dataframe.columns.values)
    return columns

def get_descriptive_statistics(dataframe):

    return